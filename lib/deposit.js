/**
 * Deposit terms, in one place.
 *
 * They appear twice — as the full section on /servicii/tatuaje and as a
 * compact notice inside the tattoo tab on /servicii — so the figures live
 * here rather than being typed into both.
 */
export const deposit = {
  min: 100,
  max: 400,
  noticeHours: 48,
  anchor: "politica-avans",
};
